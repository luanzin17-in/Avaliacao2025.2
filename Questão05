public static double mediaIdades(Pessoa[] v, int qtd) {
    if (qtd == 0) {
        return 0;
    }

    int soma = 0;

    for (int i = 0; i < qtd; i++) {
        soma += v[i].idade;
    }

    return (double) soma / qtd;
}
